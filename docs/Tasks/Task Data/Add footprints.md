---
status: Done
kanban_order: V2v
---
We should use the same shader used for the tire tracks to leave footprints on the sand.

*Done (Claude, 2026-08-12 night): every footstep now stamps a small print into the same displacement buffer the tyres write — real dents in the sand that fade like ruts do. Sampled at the foot itself, so a step onto the tarmac leaves nothing; enemies print too, since the footsteps component is shared.*
